services:
  - type: web
    name: dds-backend
    env: python
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app -w 2 -b 0.0.0.0:$PORT

  - type: web
    name: dds-frontend
    env: static
    plan: free
    rootDir: frontend
    buildCommand: ""
    staticPublishPath: .
